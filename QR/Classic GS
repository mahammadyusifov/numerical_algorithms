function [Q, R] = clgs(A, print)
    [m, n]=size(A);
    Q = zeros(m,n);
    R = zeros(n,n);
    ?? % the classical Gram-Schmidt
    if print
        disp(sprintf('||A - Q*R||_2 = %0.5e', ??)); 
        disp(sprintf('||Q''*Q - I||_2 = %0.5e', ??)); % test orthogonality of Q
    end
end
